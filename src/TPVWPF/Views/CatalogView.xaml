<UserControl x:Class="TPVWPF.Views.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TPVWPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding"
                    Value="8 4" />
            <Setter Property="MinWidth"
                    Value="100" />
            <Setter Property="FontFamily"
                    Value="Segoe UI Semibold" />
            <Setter Property="Background"
                    Value="#FF4285F4" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="Margin"
                    Value="5 0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FF3367D6" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FF2A56B7" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para carta tipo bootstrap -->
        <Style x:Key="ProductCardStyle"
               TargetType="ListBoxItem">
            <Setter Property="Padding"
                    Value="0" />
            <Setter Property="Margin"
                    Value="10" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="White"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="0"
                                Margin="10">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox"
               x:Key="SearchTextBoxStyle">
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="BorderBrush"
                    Value="#FFABADB3" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Foreground"
                    Value="#333333" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="BorderBrush"
                            Value="#FF4285F4" />
                </Trigger>
                <Trigger Property="IsFocused"
                         Value="True">
                    <Setter Property="BorderBrush"
                            Value="#FF4285F4" />
                    <Setter Property="BorderThickness"
                            Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- WrapPanel para el ItemsPanel -->
        <ItemsPanelTemplate x:Key="WrapItemsPanel">
            <WrapPanel Orientation="Horizontal"
                       HorizontalAlignment="Center" />
        </ItemsPanelTemplate>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=CatalogViewTitle}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="20 15"
                   Foreground="#333333" />

        <!-- Barra de búsqueda -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="20 0 20 15">
            <TextBox x:Name="SearchTextBox"
                     Style="{StaticResource SearchTextBoxStyle}"
                     Width="300"
                     Height="30"
                     Padding="5"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center">
            </TextBox>
        </StackPanel>

        <!-- ListBox con tarjetas -->
        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Products}"
                 Name="ProductsListBox"
                 Margin="10"
                 SelectionMode="Single"
                 SelectedItem="{Binding SelectedProduct}"
                 ItemContainerStyle="{StaticResource ProductCardStyle}"
                 ItemsPanel="{StaticResource WrapItemsPanel}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="250"
                                Margin="15">
                        <!-- Nombre de producto -->
                        <TextBlock Text="{Binding Name}"
                                   FontWeight="Bold"
                                   FontSize="16"
                                   Margin="0 0 0 5"
                                   Foreground="#333333" />

                        <!-- Precio -->
                        <TextBlock Text="{Binding Price, StringFormat=C2}"
                                   FontSize="14"
                                   Margin="0 0 0 10"
                                   Foreground="#4285F4" />

                        <!-- Botones -->
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <Button Content="{Binding Source={StaticResource LocalizationService}, Path=ProductDetailsButtonText}"
                                    Command="{Binding DataContext.ShowProductDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Content="{Binding Source={StaticResource LocalizationService}, Path=AddToCartButtonText}"
                                    Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>