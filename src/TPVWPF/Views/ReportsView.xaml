<UserControl x:Class="TPVWPF.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TPVWPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="500">

    <UserControl.Resources>
        <!-- Styles -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
            <Setter Property="Foreground"
                    Value="#333333" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
        </Style>

        <Style x:Key="ReportHeader"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="24" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Margin"
                    Value="0 0 0 30" />
            <Setter Property="Foreground"
                    Value="#333333" />
        </Style>

        <Style x:Key="ReportLabel"
               TargetType="TextBlock">
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Margin"
                    Value="0 0 15 15" />
            <Setter Property="Width"
                    Value="220" />
        </Style>

        <Style x:Key="ReportValue"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Margin"
                    Value="0 0 0 15" />
            <Setter Property="Foreground"
                    Value="#1A73E8" />
        </Style>

        <Style x:Key="CurrencyValue"
               TargetType="TextBlock"
               BasedOn="{StaticResource ReportValue}">
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="FontSize"
                    Value="15" />
        </Style>
    </UserControl.Resources>

    <Grid Background="White"
          Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=ReportsViewTitle}"
                   Style="{StaticResource ReportHeader}" />

        <!-- Datos de reporte -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">
            <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=TotalProductsLabel}"
                       Style="{StaticResource ReportLabel}" />
            <TextBlock Text="{Binding TotalProducts}"
                       Style="{StaticResource ReportValue}" />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal">
            <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=TotalSalesLabel}"
                       Style="{StaticResource ReportLabel}" />
            <TextBlock Text="{Binding TotalSales}"
                       Style="{StaticResource ReportValue}" />
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal">
            <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=HistoricalTotalRevenueLabel}"
                       Style="{StaticResource ReportLabel}" />
            <TextBlock Text="{Binding TotalRevenue, StringFormat=C2}"
                       Style="{StaticResource CurrencyValue}" />
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal">
            <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=CurrentMonthRevenueLabel}"
                       Style="{StaticResource ReportLabel}" />
            <TextBlock Text="{Binding MonthlyRevenue, StringFormat=C2}"
                       Style="{StaticResource CurrencyValue}" />
        </StackPanel>
    </Grid>
</UserControl>