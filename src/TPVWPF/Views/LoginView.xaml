<UserControl x:Class="TPVWPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TPVWPF.Views"
             xmlns:vm="clr-namespace:TPVWPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#333333" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background"
                    Value="#FFFFFF" />
            <Setter Property="BorderBrush"
                    Value="#CCCCCC" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Padding"
                    Value="5" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Height"
                    Value="30" />
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background"
                    Value="#FFFFFF" />
            <Setter Property="BorderBrush"
                    Value="#CCCCCC" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Padding"
                    Value="5" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Height"
                    Value="30" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background"
                    Value="#4CAF50" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Padding"
                    Value="10 5" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Height"
                    Value="40" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#45a049" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="#3d8b40" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <Border Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="5"
                Padding="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="300">
            <StackPanel>
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=LoginViewTitle}"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0 0 0 20"
                           HorizontalAlignment="Center"
                           Foreground="#333333" />

                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=UsernameLabel}"
                           Margin="0 0 0 5"
                           FontSize="14" />
                <TextBox Text="{Binding Username}"
                         Margin="0 0 0 15" />

                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=PasswordLabel}"
                           Margin="0 0 0 5"
                           FontSize="14" />
                <PasswordBox x:Name="PasswordBox"
                             Margin="0 0 0 15" />

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#E53935"
                           Margin="0 0 0 15"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center" />

                <Button Content="{Binding Source={StaticResource LocalizationService}, Path=LoginButton}"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>