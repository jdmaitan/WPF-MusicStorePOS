<UserControl x:Class="TPVWPF.Views.ProductAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TPVWPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
            <Setter Property="Foreground"
                    Value="#333333" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding"
                    Value="12 6" />
            <Setter Property="MinWidth"
                    Value="120" />
            <Setter Property="FontFamily"
                    Value="Segoe UI Semibold" />
            <Setter Property="Margin"
                    Value="0 0 10 0" />
            <Setter Property="Cursor"
                    Value="Hand" />
        </Style>

        <Style x:Key="PrimaryButton"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background"
                    Value="#FF4285F4" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FF3367D6" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FF2A56B7" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background"
                    Value="#FFE57373" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FFEF5350" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FFE53935" />
                </Trigger>
                <Trigger Property="IsEnabled"
                         Value="False">
                    <Setter Property="Background"
                            Value="#FFCCCCCC" />
                    <Setter Property="Foreground"
                            Value="#FF888888" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="BorderBrush"
                    Value="#EEEEEE" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="RowBackground"
                    Value="White" />
            <Setter Property="AlternatingRowBackground"
                    Value="#FAFAFA" />
            <Setter Property="GridLinesVisibility"
                    Value="None" />
            <Setter Property="HeadersVisibility"
                    Value="Column" />
            <Setter Property="ScrollViewer.CanContentScroll"
                    Value="True" />
            <Setter Property="ScrollViewer.PanningMode"
                    Value="Both" />
            <Setter Property="Stylus.IsFlicksEnabled"
                    Value="False" />
            <Setter Property="SelectionMode"
                    Value="Single" />
            <Setter Property="SelectionUnit"
                    Value="FullRow" />
            <Setter Property="AutoGenerateColumns"
                    Value="False" />
            <Setter Property="CanUserAddRows"
                    Value="False" />
            <Setter Property="CanUserDeleteRows"
                    Value="False" />
            <Setter Property="CanUserReorderColumns"
                    Value="False" />
            <Setter Property="CanUserResizeColumns"
                    Value="True" />
            <Setter Property="CanUserResizeRows"
                    Value="False" />
            <Setter Property="CanUserSortColumns"
                    Value="True" />
            <Setter Property="IsReadOnly"
                    Value="True" />
            <Setter Property="Margin"
                    Value="10" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"
                    Value="#F5F5F5" />
            <Setter Property="BorderBrush"
                    Value="#EEEEEE" />
            <Setter Property="BorderThickness"
                    Value="0 0 0 1" />
            <Setter Property="Padding"
                    Value="10 8" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Left" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="BorderBrush"
                    Value="#EEEEEE" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#F5F5F5" />
                </Trigger>
                <Trigger Property="IsSelected"
                         Value="True">
                    <Setter Property="Background"
                            Value="#E6F2FF" />
                    <Setter Property="BorderBrush"
                            Value="#B8D6FF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding"
                    Value="10 8" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}" />
        </Style>
    </UserControl.Resources>

    <Grid Background="#FFF9F9F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=ProductAdminViewTitle}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="20 15 20 10"
                   Foreground="#333333" />

        <!-- DataGrid -->
        <DataGrid Grid.Row="1"
                  Name="ProductsDataGrid"
                  ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{Binding Source={StaticResource LocalizationService}, Path=ProductIdColumnHeader}"
                                    Binding="{Binding Id}"
                                    Width="Auto" />
                <DataGridTextColumn Header="{Binding Source={StaticResource LocalizationService}, Path=ProductNameColumnHeader}"
                                    Binding="{Binding Name}"
                                    Width="*" />
                <DataGridTextColumn Header="{Binding Source={StaticResource LocalizationService}, Path=ProductDescriptionColumnHeader}"
                                    Binding="{Binding Description}"
                                    Width="*" />
                <DataGridTextColumn Header="{Binding Source={StaticResource LocalizationService}, Path=ProductPriceColumnHeader}"
                                    Binding="{Binding Price, StringFormat=C2}"
                                    Width="Auto" />
                <DataGridTextColumn Header="{Binding Source={StaticResource LocalizationService}, Path=ProductCategoryColumnHeader}"
                                    Binding="{Binding Category}"
                                    Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botónes de acción -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#EEEEEE"
                BorderThickness="0 1 0 0"
                Padding="20 15">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{Binding Source={StaticResource LocalizationService}, Path=NewProductButtonText}"
                        Command="{Binding AddNewProductCommand}"
                        Style="{StaticResource PrimaryButton}" />
                <Button Content="{Binding Source={StaticResource LocalizationService}, Path=EditProductButtonText}"
                        Command="{Binding EditSelectedProductCommand}"
                        IsEnabled="{Binding IsProductSelected}"
                        Style="{StaticResource PrimaryButton}" />
                <Button Content="{Binding Source={StaticResource LocalizationService}, Path=DeleteProductButtonText}"
                        Command="{Binding DeleteSelectedProductCommand}"
                        IsEnabled="{Binding IsProductSelected}"
                        Style="{StaticResource DeleteButton}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>