<UserControl x:Class="TPVWPF.Views.OrderDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TPVWPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
            <Setter Property="Foreground"
                    Value="#333333" />
            <Setter Property="Margin"
                    Value="0 0 0 5" />
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush"
                    Value="#EEEEEE" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Background"
                    Value="#FAFAFA" />
            <Setter Property="Padding"
                    Value="5" />
            <Setter Property="Margin"
                    Value="10" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   FontWeight="SemiBold"
                                   FontSize="14" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
                    Value="Disabled" />
            <Setter Property="Margin"
                    Value="10" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding"
                    Value="10 8" />
            <Setter Property="Margin"
                    Value="0 2" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="BorderBrush"
                    Value="#EEEEEE" />
            <Setter Property="BorderThickness"
                    Value="0 0 0 1" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#F5F5F5" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="White"
          Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=OrderDetailViewTitle}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0 0 0 10"
                   Foreground="#333333" />

        <!-- Información del cliente -->
        <GroupBox Header="{Binding Source={StaticResource LocalizationService}, Path=CustomerInformationHeader}"
                  Grid.Row="1">
            <StackPanel>
                <TextBlock Margin="8 5">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}: {1}">
                            <Binding Source="{StaticResource LocalizationService}"
                                     Path="CustomerFirstNameLabel" />
                            <Binding Path="Customer.FirstName" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Margin="8 5">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}: {1}">
                            <Binding Source="{StaticResource LocalizationService}"
                                     Path="CustomerLastNameLabel" />
                            <Binding Path="Customer.LastName" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Margin="8 5">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}: {1}">
                            <Binding Source="{StaticResource LocalizationService}"
                                     Path="CustomerAddressLabel" />
                            <Binding Path="Customer.Address" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Margin="8 5">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}: {1}">
                            <Binding Source="{StaticResource LocalizationService}"
                                     Path="CustomerPaymentMethodLabel" />
                            <Binding Path="Customer.PaymentMethod" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- Productos del pedido -->
        <ListBox Grid.Row="2"
                 Name="OrderItemsListBox"
                 ItemsSource="{Binding OrderItems}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Product.Name}"
                                   FontWeight="Bold"
                                   FontSize="14" />

                        <TextBlock Grid.Column="1"
                                   Text="{Binding Quantity, StringFormat={}x {0}}"
                                   Margin="10 0"
                                   FontSize="14" />

                        <TextBlock Grid.Column="2"
                                   Text="{Binding Subtotal, StringFormat={}{0:C2}}"
                                   HorizontalAlignment="Right"
                                   FontWeight="Bold"
                                   FontSize="14"
                                   Foreground="#4285F4" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Total del pedido -->
        <Border Grid.Row="3"
                Background="#FAFAFA"
                BorderBrush="#EEEEEE"
                BorderThickness="0 1 0 0"
                Padding="10">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=OrderTotalLabel}"
                           FontWeight="Bold"
                           FontSize="18" />
                <TextBlock Text="{Binding Total, StringFormat={}{0:C2}}"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#4285F4"
                           Margin="10 0 0 0" />
                <Button Content="{Binding Source={StaticResource LocalizationService}, Path=ExportInvoiceButton}"
                        Command="{Binding ExportInvoiceCommand}"
                        Margin="10 0 0 0"
                        Padding="10 5" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>