<UserControl x:Class="TPVWPF.Views.ProductEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TPVWPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="500">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
            <Setter Property="Foreground"
                    Value="#333333" />
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
            <Setter Property="Margin"
                    Value="15 10 5 5" />
            <Setter Property="Padding"
                    Value="0" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin"
                    Value="5 10 15 5" />
            <Setter Property="Padding"
                    Value="8" />
            <Setter Property="BorderBrush"
                    Value="#CCCCCC" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="FontSize"
                    Value="14" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding"
                    Value="12 8" />
            <Setter Property="MinWidth"
                    Value="100" />
            <Setter Property="FontFamily"
                    Value="Segoe UI Semibold" />
            <Setter Property="Margin"
                    Value="0 0 10 0" />
            <Setter Property="Cursor"
                    Value="Hand" />
        </Style>

        <Style x:Key="PrimaryButton"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background"
                    Value="#FF4285F4" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FF3367D6" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="#FF2A56B7" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Foreground"
                    Value="#333333" />
            <Setter Property="BorderBrush"
                    Value="#CCCCCC" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#F5F5F5" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="#E0E0E0" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="White"
          Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=ProductEditViewTitle}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 20"
                   Grid.ColumnSpan="2"
                   Foreground="#333333" />

        <!-- Nombre -->
        <Label Content="{Binding Source={StaticResource LocalizationService}, Path=ProductNameLabelEdit}"
               Grid.Row="1" />
        <TextBox Text="{Binding Product.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="1"
                 Grid.Column="1" />

        <!-- Descripción -->
        <Label Content="{Binding Source={StaticResource LocalizationService}, Path=ProductDescriptionLabelEdit}"
               Grid.Row="2" />
        <TextBox Text="{Binding Product.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="2"
                 Grid.Column="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinHeight="80" />

        <!-- Precio -->
        <Label Content="{Binding Source={StaticResource LocalizationService}, Path=ProductPriceLabelEdit}"
               Grid.Row="3" />
        <TextBox Text="{Binding Product.Price, Mode=TwoWay, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="3"
                 Grid.Column="1" />

        <!-- Categoría -->
        <Label Content="{Binding Source={StaticResource LocalizationService}, Path=ProductCategoryLabelEdit}"
               Grid.Row="4" />
        <TextBox Text="{Binding Product.Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="4"
                 Grid.Column="1" />

        <!-- Botones de acción -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0"
                    Grid.Row="6"
                    Grid.ColumnSpan="2">
            <Button Content="{Binding Source={StaticResource LocalizationService}, Path=SaveButtonTextEdit}"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource PrimaryButton}" />
            <Button Content="{Binding Source={StaticResource LocalizationService}, Path=CancelButtonTextEdit}"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButton}" />
        </StackPanel>
    </Grid>
</UserControl>